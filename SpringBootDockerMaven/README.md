# SpringBoot with Maven inside Docker

## Running
```bash
docker build -t springbootmaven . && docker run -p 8080:8080 -t springbootmaven
```

## Check that everything is cool
```
open http://localhost:8080
```